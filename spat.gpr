project SPAT is

   for Source_Dirs use ("src", "test");
   for Object_Dir use "obj";
   for Main use ("main_spat_test", "run_spat");

   package Compiler is
      for Switches ("ada") use ("-g");
   end Compiler;

   package Linker is
      for Switches ("ada") use ("-g");
   end Linker;

end SPAT;

